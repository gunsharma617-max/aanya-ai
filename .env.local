# Server-side only — never expose this value to the browser.
# Get a Gemini API key from Google AI Studio / your Gemini API project.
AI_API_KEY=

# Optional. Defaults to the valid Gemini model ID below when left blank.
AI_MODEL=gemini-3.5-flash
